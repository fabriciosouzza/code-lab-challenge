/**
 * Verifica se a string fornecida é um palíndromo.
 * A verificação deve ser case-insensitive e ignorar espaços,
 * pontuação e caracteres acentuados (ex: ô → o, é → e).
 *
 * Dica: use `String.prototype.normalize("NFD")` para decompor acentos
 * e uma regex para remover os caracteres diacríticos e de pontuação.
 *
 * @example
 * isPalindrome("arara") // true
 * isPalindrome("hello") // false
 * isPalindrome("A man a plan a canal Panama") // true
 * isPalindrome("Socorram-me, subi no ônibus em Marrocos") // true
 * isPalindrome("A grama é amarga") // true
 *
 * @param str - A string a ser verificada.
 * @returns `true` se for palíndromo, `false` caso contrário.
 */
export function isPalindrome(str: string): boolean {
  // TODO: Implemente a lógica para verificar palíndromo
  throw new Error("Não implementado");
}
