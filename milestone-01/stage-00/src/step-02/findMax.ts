/**
 * Encontra o maior número dentro de um array de números.
 * Não utilize `Math.max` — implemente a lógica manualmente.
 *
 * @example
 * findMax([1, 5, 3, 9, 2]) // 9
 * findMax([-10, -3, -20]) // -3
 *
 * @param numbers - Array de números a ser analisado.
 * @returns O maior número encontrado no array.
 * @throws {Error} Se o array estiver vazio, deve lançar: "O array não pode estar vazio"
 */
export function findMax(numbers: number[]): number {
  // TODO: Implemente a lógica para encontrar o maior número
  throw new Error("Não implementado");
}
