/**
 * Dado um inteiro positivo `num`, retorna `true` se ele for um quadrado perfeito,
 * ou `false` caso contrário.
 *
 * Um quadrado perfeito é um inteiro que é o quadrado de outro inteiro.
 * Em outras palavras, é o produto de algum inteiro por ele mesmo.
 *
 * Restrição: NÃO utilize funções built-in como `Math.sqrt`.
 *
 * @example
 * isPerfectSquare(16) // true  (4 * 4)
 * isPerfectSquare(14) // false
 * isPerfectSquare(1)  // true  (1 * 1)
 * isPerfectSquare(25) // true  (5 * 5)
 *
 * @param num - Um inteiro positivo a ser verificado.
 * @returns `true` se for quadrado perfeito, `false` caso contrário.
 */
export function isPerfectSquare(num: number): boolean {
  // TODO: Implemente sem usar Math.sqrt
  throw new Error("Não implementado");
}
