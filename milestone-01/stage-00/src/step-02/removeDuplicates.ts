/**
 * Remove elementos duplicados de um array, retornando um novo array
 * apenas com os valores únicos, preservando a ordem original.
 *
 * @example
 * removeDuplicates([1, 2, 2, 3, 4, 4, 5]) // [1, 2, 3, 4, 5]
 * removeDuplicates(["a", "b", "a", "c"]) // ["a", "b", "c"]
 *
 * @param items - Array com possíveis duplicatas.
 * @returns Um novo array sem valores duplicados.
 */
export function removeDuplicates<T>(items: T[]): T[] {
  // TODO: Implemente a lógica para remover duplicatas
  throw new Error("Não implementado");
}
