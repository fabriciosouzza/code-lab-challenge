/**
 * Implementa o clássico FizzBuzz.
 * Retorna um array de strings de 1 até `n` (inclusive), onde:
 * - Múltiplos de 3 são substituídos por "Fizz"
 * - Múltiplos de 5 são substituídos por "Buzz"
 * - Múltiplos de 3 e 5 são substituídos por "FizzBuzz"
 * - Demais números são retornados como string
 *
 * @example
 * fizzBuzz(5) // ["1", "2", "Fizz", "4", "Buzz"]
 *
 * @param n - O número limite (inclusive).
 * @returns Um array de strings com o resultado do FizzBuzz.
 */
export function fizzBuzz(n: number): string[] {
  // TODO: Implemente a lógica do FizzBuzz
  throw new Error("Não implementado");
}
