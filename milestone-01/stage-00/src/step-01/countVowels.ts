/**
 * Conta o número de vogais presentes na string.
 * Vogais consideradas: a, e, i, o, u (incluindo acentuadas: á, é, í, ó, ú, â, ê, ô, etc.).
 * A contagem deve ser case-insensitive.
 *
 * Dica: normalize a string com `normalize("NFD")` para decompor acentos,
 * e depois conte apenas as vogais base (a, e, i, o, u).
 *
 * @example
 * countVowels("hello") // 2
 * countVowels("TypeScript é um superset") // 8
 * countVowels("aeiou") // 5
 * countVowels("Rhythm") // 0
 *
 * @param str - A string a ser analisada.
 * @returns O número total de vogais encontradas.
 */
export function countVowels(str: string): number {
  // TODO: Implemente a lógica para contar vogais
  throw new Error("Não implementado");
}
