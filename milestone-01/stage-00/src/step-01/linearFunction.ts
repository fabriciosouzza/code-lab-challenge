/**
 * Calcula o resultado de uma função linear.
 *
 * @example
 * linearFunction(2, 3, 5)  // 13
 * linearFunction(1, 0, 10) // 10
 * linearFunction(-1, 4, 2) // 2
 *
 * @param a - O coeficiente angular (slope).
 * @param b - O coeficiente linear (intercept).
 * @param x - O valor de entrada.
 * @returns O resultado.
 */
export function linearFunction(a: number, b: number, x: number): number {
  // TODO: Implemente o cálculo da função linear
  throw new Error("Não implementado");
}
