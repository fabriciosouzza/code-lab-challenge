/**
 * Troca os valores das propriedades `a` e `b` do objeto recebido.
 * A troca deve ser feita **in-place** (modificando o objeto original),
 * utilizando uma terceira variável auxiliar para não perder nenhum valor.
 *
 * @example
 * const pair = { a: 1, b: 2 };
 * swapValues(pair);
 * // pair.a === 2, pair.b === 1
 *
 * @param pair - Objeto com as propriedades `a` e `b` a serem trocadas.
 */
export function swapValues(pair: { a: number; b: number }): void {
  // TODO: Troque pair.a e pair.b
  throw new Error("Não implementado");
}
