/**
 * Calcula a soma de todos os números em um array.
 *
 * @example
 * sumArray([1, 2, 3, 4, 5]) // 15
 * sumArray([]) // 0
 * sumArray([-1, 1]) // 0
 *
 * @param numbers - Array de números a serem somados.
 * @returns A soma total dos números.
 */
export function sumArray(numbers: number[]): number {
  // TODO: Implemente a lógica para somar o array
  throw new Error("Não implementado");
}
