/**
 * Capitaliza a primeira letra de cada palavra na string.
 *
 * @example
 * capitalizeWords("hello world") // "Hello World"
 * capitalizeWords("o rato roeu a roupa") // "O Rato Roeu A Roupa"
 *
 * @param str - A frase a ser capitalizada.
 * @returns A frase com a primeira letra de cada palavra em maiúscula.
 */
export function capitalizeWords(str: string): string {
  // TODO: Implemente a lógica para capitalizar cada palavra
  throw new Error("Não implementado");
}
